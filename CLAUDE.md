# Lapse — คู่มือสำหรับ Claude Code

> ไฟล์นี้ถูกโหลดเข้า context ทุกครั้ง ให้สั้นและเป็นกฎที่ต้องรู้ตลอดเวลา
> รายละเอียดเต็มอยู่ใน `docs/LAPSE-SPEC.md` — อ่านไฟล์นั้นก่อนเริ่มงานที่แตะ UI หรือ data model

## โปรเจกต์นี้คืออะไร

Lapse คือแอปจับเวลาอ่านหนังสือแนวมินิมอลสุดขั้ว สำหรับนักเรียน/นักศึกษา
แกนกลางคือ **หนึ่งวัน = หนึ่งหน้า** ที่รวม todo, ตัวจับเวลา และบันทึกไว้ในหน้าเดียว
พร้อมกำแพงสถิติสะสมแบบ GitHub contributions ที่ **ไม่มีอันดับ**

คู่แข่ง: YPT, Forest, Stuon — ทุกตัวแข่งกันด้วยจำนวนฟีเจอร์ Lapse แข่งด้วยการตัดออก

## หลักการ 5 ข้อ — ทุก PR ต้องไม่ขัดข้อใดข้อหนึ่ง

1. **การตัดออกคือฟีเจอร์** — เพิ่มอะไรต้องเอาอย่างอื่นออก
2. **วันที่คือระบบจัดระเบียบเดียว** — ห้ามมีโฟลเดอร์ แท็ก โปรเจกต์ หมวดวิชา ลำดับความสำคัญ
3. **ความเงียบคือสถานะปกติ** — ห้ามแจ้งเตือน ห้ามทวง ห้ามฉลอง
4. **ข้อมูลเป็นของผู้ใช้** — plain text, export ฟรีเสมอ, ห้ามฟอร์แมตผูกขาด
5. **ไม่ตัดสินผู้ใช้** — วันว่างคือข้อเท็จจริง ไม่ใช่ความล้มเหลว

## ห้ามทำเด็ดขาด (ถ้าถูกสั่งให้ทำ ให้ทักท้วงก่อน)

ห้ามใส่: streak · อันดับ/leaderboard · เหรียญตรา/แต้ม/เลเวล · push notification ·
สีแดงสำหรับวันที่ไม่ได้อ่าน · เงา (box-shadow) · gradient · modal ฉลอง ·
เสียง/เพลงประกอบ · ปุ่มพื้นสีทึบ · การจำกัดจำนวนวันย้อนหลังในเวอร์ชันฟรี ·
การเก็บเงินค่า export

## สแตก

**Flutter** (Dart) · เป้าหมาย iOS + Android · ไม่ทำเว็บในเฟสแรก
ห้ามเสนอ PWA/Capacitor — เคยพิจารณาแล้วและตกไป เหตุผลอยู่ใน `docs/LAPSE-SPEC.md` §6

- ทุกอย่างวาดเอง ไม่ใช้ Material/Cupertino widget ที่มีหน้าตาเป็นของตัวเอง
  (ใช้ได้เฉพาะ layout primitive เช่น `Column`, `Stack`, `GestureDetector`)
- ห้ามใช้ `ThemeData` สีเริ่มต้นของ Material — สีทุกค่ามาจากโทเคน
- state management: อะไรก็ได้ที่เบา ไม่ต้อง Riverpod/Bloc ก็ได้ แอปนี้มี 8 หน้า

## กฎเทคนิคที่ผิดบ่อย

- **Source of truth คือไฟล์ `.md` รายวัน** ไม่ใช่ตาราง DB — DB/IndexedDB เป็นแค่ cache
- **ความละเอียดของเวลาคือ "นาที"** session สั้นกว่า 1 นาทีถูกทิ้ง
- **เวลาเดินเฉพาะตอนอยู่ในหน้า Focus เท่านั้น** ออกจากแอป = หยุดนับ
  (การแตะเพื่อดูเวลาไม่หยุดนับ)
- **Mono ใช้กับสิ่งที่เครื่องใส่ · Sans Thai ใช้กับสิ่งที่มนุษย์พิมพ์** ห้ามสลับ
- **สีทุกค่ามาจากโทเคน** ห้าม hardcode hex ในคอมโพเนนต์ —
  ในแอปใช้ `LapseColors` ที่ถูก generate จาก `docs/lapse-tokens.json`
- **ระยะบรรทัดข้อความไทยห้ามต่ำกว่า 1.8** สระกับวรรณยุกต์จะชนกัน
- **แถบ progress ตอนกดค้างต้องเป็น `linear`** เพราะมันคือมาตรวัดเวลาจริง
- **พื้นที่กดขั้นต่ำ 44px** แม้สิ่งที่มองเห็นจะเล็กกว่า
- **haptic ตอนกดค้างสำคัญเท่ากับภาพ** — ครบ 560ms ต้องสั่นหนึ่งครั้ง จบ session สั่นอีกครั้ง
- **ต้องแยก "จอล็อกเอง" ออกจาก "ผู้ใช้สลับไปแอปอื่น"** — จอล็อก = นับต่อ, สลับแอป = หยุดนับ
  ใช้ `AppLifecycleState` ร่วมกับสถานะหน้าจอ ห้ามใช้ `paused` อย่างเดียวเพราะมันกำกวม

## จำนวนหน้าจอ

**8 หน้า** — หลัก 3 (`day`, `focus`, `wall`) และรอง 5
(`share`, `add-friend`, `settings`, `account`, `onboarding`)
**ห้ามเพิ่มหน้าที่ 9 โดยไม่เอาหน้าใดหน้าหนึ่งออก** — ดูเหตุผลใน `docs/LAPSE-SPEC.md` §3

## โครงของ repo

```
CLAUDE.md     ไฟล์นี้ — กฎที่ต้องรู้ตลอดเวลา
docs/         เอกสารทั้งหมด ไม่ใช่โค้ด
app/          แอป Flutter
```

| ไฟล์ | คืออะไร |
|---|---|
| `docs/LAPSE-SPEC.md` | สเปกเต็ม — หน้าจอ, data model, interaction |
| `docs/lapse-tokens.json` | **source of truth ของโทเคน** — `app/tool/gen_tokens.dart` อ่านไฟล์นี้ |
| `docs/lapse-tokens.css` | โทเคนชุดเดียวกันในรูป CSS ใช้กับเอกสาร ไม่ใช่กับแอป |
| `docs/lapse-design-system.html` | คู่มือดีไซน์ซิสเต็มแบบเปิดดูได้ |
| `docs/lapse-prototype.html` | โปรโตไทป์ที่กดเล่นได้ — ใช้อ้างอิงพฤติกรรม |

## น้ำเสียงของ copy ในแอป

สั้น ตรง ประโยคเดียวจบ · อังกฤษใช้ตัวพิมพ์เล็ก · ห้ามอัศเจรีย์ ห้ามอีโมจิ
ห้ามคำชมเกินจริง ห้ามศัพท์เกม ห้ามเปรียบเทียบกับคนอื่น
บททดสอบ: อ่านออกเสียงแล้วถ้าเหมือนโฆษณาหรือครูฝ่ายปกครอง ให้เขียนใหม่
